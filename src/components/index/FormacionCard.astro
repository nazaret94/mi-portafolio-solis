---
// Props esperados: { logo, alt }
const { logo, alt } = Astro.props;
---

<div class="bg-rosa-1 rounded-2xl flex items-center justify-center h-70 w-70 shadow-md">
  <img src={logo} alt={alt} class="max-h-full max-w-full object-contain" />
</div>